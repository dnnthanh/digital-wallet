name: digital-wallet
services:
  auth-keygen:
    extends:
      file: compose/backend/auth.yml
      service: auth-keygen
  be-auth-api:
    extends:
      file: compose/backend/auth.yml
      service: be-auth-api
volumes:
  auth-client-key:
networks:
  wallet:
    name: digital-wallet-network
